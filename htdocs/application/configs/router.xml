<?xml version="1.0" encoding="UTF-8"?>
<routes>

    <index>
        <route></route>
        <defaults>
            <module>auctions</module>
            <controller>index</controller>
            <action>index</action>
        </defaults>
        <map>
        </map>
        <reverse></reverse>
    </index>

    <login>
        <route>login</route>
        <defaults>
            <module>auctions</module>
            <controller>auth</controller>
            <action>index</action>
        </defaults>
        <map>
        </map>
        <reverse></reverse>
    </login>
    
    <processLogin>
        <route>login/process</route>
        <defaults>
            <module>auctions</module>
            <controller>auth</controller>
            <action>process</action>
        </defaults>
        <map>
        </map>
        <reverse></reverse>
    </processLogin>
    
    <logout>
        <route>logout</route>
        <defaults>
            <module>auctions</module>
            <controller>auth</controller>
            <action>logout</action>
        </defaults>
        <map>
        </map>
        <reverse></reverse>
    </logout>
    
    <registrationForm>
        <route>registration</route>
        <defaults>
            <module>auctions</module>
            <controller>user</controller>
            <action>registration</action>
        </defaults>
        <map>
        </map>
        <reverse></reverse>
    </registrationForm>
    
    <processRegistration>
        <route>user/processregistration</route>
        <defaults>
            <module>auctions</module>
            <controller>user</controller>
            <action>processRegistration</action>
        </defaults>
        <map>
        </map>
        <reverse></reverse>
    </processRegistration>
    
    <registerUser>
        <route>register/(\a{40})</route>
        <defaults>
            <module>auctions</module>
            <controller>user</controller>
            <action>processRegistration</action>
        </defaults>
        <map>
            <secretCode>1</secretCode>
        </map>
        <reverse>registration</reverse>
        
    </registerUser>
    
</routes>
